## IDF Component Manager Manifest File
dependencies:
  espressif/esp_tinyusb:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=5.0"
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
